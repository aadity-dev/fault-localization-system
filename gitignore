# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
env/
ENV/
.venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# --- Environment / secrets ---
.env
.env.local
*.pem
*.key

# --- Database ---
*.db
*.sqlite
*.sqlite3
test_seed.db

# --- Backend local-dev data copy ---
# Root-level /data is the source of truth (generated by simulator/generate_grid.py
# and committed). backend/data/ is a local convenience copy used during development
# before docker-compose existed — see DECISIONS.md. Not duplicated in git.
backend/data/

# --- Logs ---
*.log
server.log
nohup.out

# --- OS / editor cruft ---
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp

# --- Docker ---
# (docker-compose.yml itself IS committed — only build/runtime artifacts ignored)
**/__pycache__/

# --- Frontend / Node (if any JS tooling gets added) ---
node_modules/
dist/
build/

# --- Streamlit ---
.streamlit/secrets.toml

# --- Coverage / test artifacts ---
.coverage
htmlcov/
coverage.xml

# --- Jupyter (if used for exploration/debugging) ---
.ipynb_checkpoints/
*.ipynb_checkpoints
